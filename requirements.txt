ultralytics>=8.4
torch>=2.2
torchvision>=0.17
opencv-python>=4.9
gradio>=5.0
matplotlib>=3.7
seaborn>=0.12
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
pytest>=8.0
httpx>=0.27

# Only needed to regenerate/run notebooks/01_eda.ipynb interactively.
jupyter>=1.0
nbformat>=5.10
nbconvert>=7.16
ipykernel>=6.29

# --- Notes on installation ---
# This file intentionally does NOT pin a CUDA-specific torch build (no "+cu121"-style
# index URL) so it installs cleanly on both the CUDA training machine and an Apple
# Silicon (MPS) machine used for inference/demo.
#
# For GPU-accelerated training on an NVIDIA machine, install torch/torchvision from the
# appropriate CUDA wheel index BEFORE installing the rest of this file, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# On Apple Silicon (macOS), the default `pip install torch` build already includes MPS
# support -- no special index needed.
